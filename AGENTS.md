# 工程守则（AI Agent 必读）

本文件由 2026-08-14 全量审计（30+ 处缺陷）沉淀。任何 AI 在修改本仓库前必须通读，修改后必须逐条对照自检。

## 零、写代码前 4 问（默认假设必须是悲观的）

1. **"如果这一步坏了，谁会发现？"** —— 答不上来 = 静默失效，重写。这个系统的 30+ 缺陷里，最贵的那个是：推送失败 10 天无人知晓。
2. **"我依赖的这个成功信号，真的代表用户收到了吗？"** —— exit 0 ≠ 送达，workflow SUCCESS ≠ 内容送达。信号源必须是真实证据（如推送返回码），不是"没报错"。
3. **"我验证的环境，和部署的环境，哪里不一样？"** —— 依赖清单、编码、时区、网络、权限，逐项核对。本地能跑不是证据，部署环境跑通才是。
4. **"我的修复，是在补信号，还是在补信号源？"** —— 补丁盖在坏信号上 = 补丁永不触发（历史：重试+退避加在恒 exit 0 的 main.py 上，形同虚设）。

## 一、信号完整性（本仓库历史上最贵的教训）

1. **退出码是唯一的失败信号**：main.py 推送失败必须非零退出（sys.exit(1)）。CI 重试循环、看门狗补推全部依赖它。任何"推送失败只打日志不退出"的写法都是静默失效——用户连续多天收不到日报且无人知晓（8/4 起发生过 10 天）。
2. **"成功"必须有真实证据**：workflow 显示 SUCCESS ≠ 内容送达。验证一条链路时，问自己：这个信号是从哪来的？它真的代表用户看到了内容吗？（push_marker.json 就是为了解决"run 被杀但已送达"的双推问题）
3. **不要修补在坏信号上**：加"重试"前先确认 main.py 会不会非零退出；加"看门狗"前先确认它判定的依据可靠。补丁盖在错误信号上 = 补丁永不触发。

## 二、状态一致性（每天跑的状态系统）

4. **"已送达"是落盘的唯一前提**：拆解计数、机会库 times_seen、溢池、去重标记——一切状态更新在推送确认成功后才落盘。失败日保持原状，明天可重做。
5. **标记粒度必须精确**：mark_seen 只标记实际送达的条目（按子卡粒度），绝不用"任一模块成功"标记全部内容——否则失败模块的内容永久丢失。
6. **dry-run 绝不落盘**：演练碰真实 storage 是污染（历史教训：演练写白名单、写溢池，正式运行被带偏）。OPC_STORAGE_DIR 隔离必须覆盖所有路径，新增持久化文件时必须检查。

## 三、环境等同性

7. **本地能跑 ≠ CI 能跑**：依赖声明（requirements.txt）、workflow 安装清单、实际 import 三者必须一致。任何新 import 必须同步三处。本地 venv 里有的包，CI 上可能没有（历史：Twitter 源在 CI 上必然 0 条持续数天）。
8. **验证部署环境**：改动后必须在 GitHub Actions 真实跑一次，看日志确认，而不是本地测试绿就交付。

## 四、AI 契约防御（模型输出不可信）

9. **模型输出按最坏情况写代码**：裸对象当数组、数字返回成字符串、缺 index、截断 JSON、inf/NaN——全部要有兜底。解析失败的条目标记未处理（进统计），绝不静默判"不相关"。

## 五、过滤器/关键词

10. **否定词只收词组不收单字**：单字"非""零"会错误豁免"非常需要编程"。补技术词时检查英文裸词（coding/developer 等）。
11. **阈值一致**：反馈的"同类"判定必须与机会库归并使用同一闸门（_is_same_topic），两套阈值会让点击反馈跨主题泄漏。

## 六、运行通道

12. **单一运行通道**：推送只走 GitHub Actions 一条通道，禁止另建本地定时任务/副本运行（双通道各自独立去重=每天重复推送；本地定时设施曾因配置占位符静默失败 10 天，2026-08-25 已移除）。
13. **时区明确**：时间戳一律北京时间（CST）；schedule 按进程本地时区，部署在 UTC 容器要换算。

## 七、提交纪律

14. 一次修复 = 一个主题;改完必须:①全量测试绿 ②dry-run 演练 ③CI 真实跑一次 ④与审查员交叉验证再交付。
15. 本仓库有两个 agent 可能并发工作:push 前先 pull --rebase,冲突时保留"更完整、经过审查"的实现,并说明取舍。

## 八、失败模式档案（本仓库真实发生过的 bug 模式 + 如何在自己代码里发现）

| 模式 | 真实案例 | 检测方法（写代码时自查） |
|---|---|---|
| **静默死亡** | 推送失败恒 exit 0 → 重试/看门狗全部空转,系统死 10 天无人知晓 | 对每个失败路径问:错误被谁看到?写日志不够——日志没人看等于没写。失败必须改变退出码/状态/通知之一 |
| **环境等同** | twitter.py 用 requests 但 workflow 不装 → CI 上国际主力源必然 0 条数天 | 新 import 依赖必须同步三处:requirements.txt / workflow pip install / 实际 import。改动后必须在部署环境真实跑一次 |
| **状态先于送达** | 拆解计数/机会库 times_seen 在推送前落盘 → 失败日计数虚增,连挂 3 天触发假"红海" | 任何"记账"操作,问:如果后续环节失败,账能回滚吗?不能 → 延迟到确认成功后再记 |
| **契约假设** | AI 返回裸对象/缺 index/字符串数字/截断 JSON → 静默丢条或整轮崩溃 | 所有外部输入(AI 输出/网络响应/文件内容)按最坏情况写:类型不匹配、字段缺失、越界、非法值,每个都要有兜底,且兜底路径要打日志 |
| **部分成功全量标记** | 任一模块推送成功 → 全部内容标已读 → 失败模块内容永久丢失 | 标记粒度和"送达粒度"必须一致。用"任一成功"标记"全部"= 丢内容 |
| **幂等缺失** | 超时被杀前已推送成功 → 看门狗补推 → 用户收到两份相同日报 | 重试/补推类逻辑必须有"已成功"的持久化证据(如 push_marker.json),而非猜状态 |
| **路径漂移** | run_bot.bat 硬编码 WorkBuddy 副本 → 双副本双跑、状态分裂、假告警 | 任何脚本的路径用相对/动态解析(%~dp0/__file__),禁止硬编码绝对路径;单一运行通道 |
